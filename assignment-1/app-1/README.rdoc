== README

About this project
———————————————————
Assignment 1:
Create a new Rails application from scratch with one controller that has at least three views.


Project method
———————————————————
App-1 includes a welcome controller with three views: index, single_view, and profile_view. The two later views are rendered using a link_to helper from the index view.